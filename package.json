{
  "name": "meow-mine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "mkdir -p public && cp meow_mine.html public/meow_mine.html && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "vite": "latest"
  },
  "devDependencies": {}
}
